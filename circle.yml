machine:
  environment:
  node:
    version: 6
dependencies:
  override:
    - npm i
compile:
  override:
test:
  override:
deployment:
  production:
    branch: master
    commands:
      - npm run deploy
